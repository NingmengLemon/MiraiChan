[project]
name = "mbplugin_rebootable"
version = "1.0.0"
description = "A plugin for restarting the bot via command."
readme = "readme.md"
requires-python = ">=3.12"
dependencies = [
    "lemony_checkers",
    "lemony_settings",
    "melobot[onebot]>=3.5.0",
]


[tool.uv.sources]
lemony_checkers = { workspace = true }
lemony_settings = { workspace = true }
